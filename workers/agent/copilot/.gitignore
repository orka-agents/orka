# Bundled Copilot CLI artifacts — generated by the copilot-sdk bundler.
# These are large platform-specific binaries that should NOT be committed.
# They are regenerated at docker build time or via `make bundle-copilot-cli`.
*.zst
*.license
zcopilot_*.go
