name: Security Scan E2E

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".dockerignore"
      - ".github/workflows/security-scan-e2e.yml"
      - "Dockerfile"
      - "Makefile"
      - "api/**"
      - "cmd/**"
      - "config/**"
      - "go.mod"
      - "go.sum"
      - "hack/**"
      - "internal/**"
      - "pkg/**"
      - "scripts/lib/e2e-admission-tls.sh"
      - "scripts/lib/ensure-static-mode-namespace.sh"
      - "scripts/lib/kind-local-registry.sh"
      - "scripts/fixtures/security-scan-fake-acp/**"
      - "scripts/security-scan-e2e.sh"
      - "scripts/tests/security-scan-e2e-test.sh"
      - "workers/**"
  push:
    paths:
      - ".dockerignore"
      - ".github/workflows/security-scan-e2e.yml"
      - "Dockerfile"
      - "Makefile"
      - "api/**"
      - "cmd/**"
      - "config/**"
      - "go.mod"
      - "go.sum"
      - "hack/**"
      - "internal/**"
      - "pkg/**"
      - "scripts/lib/e2e-admission-tls.sh"
      - "scripts/lib/ensure-static-mode-namespace.sh"
      - "scripts/lib/kind-local-registry.sh"
      - "scripts/fixtures/security-scan-fake-acp/**"
      - "scripts/security-scan-e2e.sh"
      - "scripts/tests/security-scan-e2e-test.sh"
      - "workers/**"

permissions:
  contents: read

concurrency:
  group: security-scan-e2e-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  security-scan-e2e:
    name: Security Scan E2E
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      KIND_CLUSTER: orka-security-scan-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.27'
          check-latest: true

      - name: Install kind
        uses: ./.github/actions/setup-kind

      - name: Install prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Verify toolchain
        run: |
          kind version
          kubectl version --client=true
          jq --version

      - name: Verify security scan harness-v2 assertions
        run: |
          bash -n scripts/security-scan-e2e.sh scripts/tests/security-scan-e2e-test.sh
          bash scripts/tests/security-scan-e2e-test.sh

      - name: Run security scan e2e
        env:
          ORKA_SECURITY_SCAN_DIAGNOSTICS_DIR: ${{ runner.temp }}/security-scan-e2e-diagnostics
        run: bash scripts/security-scan-e2e.sh

      - name: Upload security scan failure diagnostics
        if: ${{ failure() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: security-scan-e2e-diagnostics-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/security-scan-e2e-diagnostics
          if-no-files-found: warn
          retention-days: 14
