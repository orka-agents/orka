interface:
  display_name: "Orka Kind Deploy"
  short_description: "Safely rebuild and deploy Orka to kind"
  default_prompt: "Use $orka-kind-deploy with the repo-scoped kindctl kubeconfig to rebuild all Orka images, load them into the existing exact kind cluster, render without mutating the worktree, and verify controller plus harness-wrapper rollouts."
