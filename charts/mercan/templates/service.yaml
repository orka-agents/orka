apiVersion: v1
kind: Service
metadata:
  name: {{ include "mercan.fullname" . }}
  labels:
    {{- include "mercan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
    - port: {{ .Values.controller.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "mercan.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: controller
