---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
    gateway.orka.ai/session-cutoff-schema: v1
  name: tasks.core.orka.ai
spec:
  group: core.orka.ai
  names:
    kind: Task
    listKind: TaskList
    plural: tasks
    singular: task
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Task is the Schema for the tasks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TaskSpec defines the desired state of Task
            properties:
              agentRef:
                description: AgentRef references an Agent CRD for configuration
                properties:
                  name:
                    description: Name is the name of the Agent
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Agent (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              agentRuntime:
                description: AgentRuntime contains task-level overrides for agent
                  runtime configuration (when type is "agent")
                properties:
                  allowBash:
                    description: AllowBash enables the agent to run bash commands
                      (overrides Agent default)
                    type: boolean
                  allowedTools:
                    description: AllowedTools lists the tools the agent is allowed
                      to use (overrides Agent defaults)
                    items:
                      type: string
                    type: array
                  disallowedTools:
                    description: DisallowedTools lists tools the agent is not allowed
                      to use
                    items:
                      type: string
                    type: array
                  maxTurns:
                    description: MaxTurns limits the number of agent loop iterations
                    format: int32
                    maximum: 1000
                    minimum: 1
                    type: integer
                  workspace:
                    description: |-
                      Workspace is the legacy harness v1 agent workspace configuration at its
                      historical JSON path. It is a preserved compatibility read surface for
                      stored v1 Tasks only: it can never be introduced or changed, and it is
                      not an authority surface for new work.
                    properties:
                      branch:
                        description: Branch is the git branch to checkout.
                        type: string
                      forkRepo:
                        description: ForkRepo is the writable fork repository URL
                          for pushing changes.
                        type: string
                      gitRepo:
                        description: GitRepo is the repository URL to clone.
                        type: string
                      gitSecretRef:
                        description: |-
                          GitSecretRef references a Secret containing git credentials. Adopted
                          legacy bindings freeze the exact Secret identity; new bindings reject it.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      prBaseBranch:
                        description: PRBaseBranch is the upstream branch to target
                          for pull requests.
                        type: string
                      pushBranch:
                        description: |-
                          PushBranch is the remote branch name to push changes to after the agent
                          completes.
                        type: string
                      ref:
                        description: Ref is a specific git ref (commit SHA, tag) to
                          checkout.
                        type: string
                      subPath:
                        description: SubPath is a subdirectory within the repo to
                          use as workspace root.
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: legacy agentRuntime.workspace is a preserved harness v1
                    compatibility surface; new Tasks must use spec.workspace
                  optionalOldSelf: true
                  rule: '!has(self.workspace) || (oldSelf.hasValue() && has(oldSelf.value().workspace)
                    && self.workspace == oldSelf.value().workspace)'
              ai:
                description: AI contains AI-specific configuration (when type is "ai")
                properties:
                  maxTokens:
                    description: MaxTokens limits the response length
                    format: int32
                    type: integer
                  model:
                    description: Model is the model identifier - required if providerRef
                      not set
                    type: string
                  prompt:
                    description: Prompt is the user prompt for the AI task
                    type: string
                  provider:
                    description: Provider is the LLM provider (anthropic, openai)
                      - required if providerRef not set
                    enum:
                    - anthropic
                    - openai
                    type: string
                  providerRef:
                    description: |-
                      ProviderRef references a Provider CRD for LLM configuration
                      If set, provider and model fields are optional (defaults from Provider)
                    properties:
                      name:
                        description: Name is the name of the Provider
                        type: string
                      namespace:
                        description: Namespace is the namespace of the Provider (defaults
                          to Task namespace)
                        type: string
                    required:
                    - name
                    type: object
                  skills:
                    description: Skills references Skill CRDs to inject into the agent's
                      system prompt
                    items:
                      description: SkillReference references a Skill CRD by name or
                        inline skill content from a ConfigMap key.
                      properties:
                        configMapRef:
                          description: ConfigMapRef references a ConfigMap key containing
                            skill text
                          properties:
                            key:
                              description: Key is the key within the ConfigMap
                              type: string
                            name:
                              description: Name is the name of the ConfigMap
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        name:
                          description: Name references a Skill CR by name
                          type: string
                      type: object
                    type: array
                  systemPrompt:
                    description: SystemPrompt is an optional system prompt
                    type: string
                  temperature:
                    description: Temperature controls randomness in generation
                    maximum: 2
                    minimum: 0
                    type: number
                  tools:
                    description: Tools lists the tools available for this task
                    items:
                      type: string
                    type: array
                type: object
              args:
                description: Args are the arguments to pass to the command
                items:
                  type: string
                type: array
              command:
                description: Command is the command to run in the container
                items:
                  type: string
                type: array
              concurrencyPolicy:
                default: Forbid
                description: ConcurrencyPolicy specifies how to treat concurrent runs
                  (Allow or Forbid).
                enum:
                - Allow
                - Forbid
                type: string
              env:
                description: Env is a list of environment variables to set in the
                  container
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              execution:
                description: Execution defines worker pod runtime and placement settings.
                properties:
                  affinity:
                    description: Affinity defines Kubernetes affinity and anti-affinity
                      rules for worker pods.
                    properties:
                      nodeAffinity:
                        description: Describes node affinity scheduling rules for
                          the pod.
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node matches the corresponding matchExpressions; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: |-
                                An empty preferred scheduling term matches all objects with implicit weight 0
                                (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                              properties:
                                preference:
                                  description: A node selector term, associated with
                                    the corresponding weight.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                weight:
                                  description: Weight associated with matching the
                                    corresponding nodeSelectorTerm, in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - preference
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to an update), the system
                              may or may not try to eventually evict the pod from its node.
                            properties:
                              nodeSelectorTerms:
                                description: Required. A list of node selector terms.
                                  The terms are ORed.
                                items:
                                  description: |-
                                    A null or empty node selector term matches no objects. The requirements of
                                    them are ANDed.
                                    The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - nodeSelectorTerms
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      podAffinity:
                        description: Describes pod affinity scheduling rules (e.g.
                          co-locate this pod in the same node, zone, etc. as some
                          other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      podAntiAffinity:
                        description: Describes pod anti-affinity scheduling rules
                          (e.g. avoid putting this pod in the same node, zone, etc.
                          as some other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the anti-affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling anti-affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and subtracting
                              "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the anti-affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the anti-affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector constrains worker pods to nodes with
                      matching labels.
                    type: object
                  runtimeClassName:
                    description: RuntimeClassName routes worker pods through a specific
                      RuntimeClass.
                    type: string
                  tolerations:
                    description: Tolerations allows worker pods to schedule onto tainted
                      nodes.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  workspace:
                    description: |-
                      Workspace requests an execution workspace for worker-backed Task types.
                      ACP core agent Tasks reject this field because their ephemeral workspace is
                      owned by RuntimeSession lifecycle and clean-room publication. Actor-backed
                      RuntimeSession support is a future integration behind the v2 lifecycle seam.
                    properties:
                      boot:
                        description: |-
                          Boot asks providers that support it to boot the workspace workload from scratch
                          instead of resuming from the provider's default snapshot. Currently supported
                          by the Substrate provider.
                        type: boolean
                      classRef:
                        description: |-
                          ClassRef selects an immutable ExecutionWorkspaceClass in the Task namespace. Setting
                          classRef implicitly enables the controller-first workspace path.
                        properties:
                          name:
                            description: Name is the class name.
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                      cleanupPolicy:
                        description: |-
                          CleanupPolicy controls whether the workspace is deleted or retained after use.
                          Defaults to delete when omitted.
                        enum:
                        - delete
                        - retain
                        type: string
                      enabled:
                        default: false
                        description: Enabled requests use of a durable workspace for
                          the task execution.
                        type: boolean
                      hibernation:
                        description: |-
                          Hibernation configures process lifetime inside the workspace. Resident
                          mode is currently rejected until the worker protocol can report per-turn
                          completion separately from resident process lifetime.
                        properties:
                          processMode:
                            description: |-
                              ProcessMode controls whether each turn starts fresh or reuses a resident
                              process. Defaults to fresh.
                            enum:
                            - fresh
                            - resident
                            type: string
                          residentKey:
                            description: |-
                              ResidentKey selects the process slot for resident mode. When omitted, the
                              worker derives a stable key from namespace, template, and reuse key.
                            type: string
                        type: object
                      onDetach:
                        description: OnDetach requests an action allowed by the selected
                          class.
                        enum:
                        - Suspend
                        - Delete
                        type: string
                      poolRef:
                        description: |-
                          PoolRef references an operator-managed Substrate actor pool for placement,
                          density tracking, and oversubscription policy.
                        properties:
                          name:
                            description: Name is the pool name.
                            type: string
                          namespace:
                            description: Namespace is the pool namespace. It defaults
                              to the Task namespace.
                            type: string
                        type: object
                      provider:
                        description: |-
                          Provider selects the workspace backend. When omitted, the controller
                          resolves the configured default workspace provider; the built-in
                          compatibility default is agent-sandbox.
                        enum:
                        - agent-sandbox
                        - substrate
                        type: string
                      reusePolicy:
                        default: none
                        description: |-
                          ReusePolicy controls whether the workspace is fresh or session-scoped.
                          Defaults to none when omitted.
                        enum:
                        - none
                        - session
                        type: string
                      snapshot:
                        description: |-
                          Snapshot configures explicit provider snapshot restore/checkpoint behavior.
                          Non-empty settings are currently rejected until provider checkpoint/restore
                          support is available through Orka.
                        properties:
                          checkpointOnRelease:
                            description: |-
                              CheckpointOnRelease requests a checkpoint when cleanup releases or retains
                              the workspace. CheckpointURI must be set when this is true.
                            type: boolean
                          checkpointURI:
                            description: |-
                              CheckpointURI is a provider-native snapshot URI prefix to write when the
                              workspace is retained or released.
                            type: string
                          restoreURI:
                            description: |-
                              RestoreURI is a provider-native snapshot URI prefix to restore before the
                              workspace command runs.
                            type: string
                        type: object
                      templateRef:
                        description: |-
                          TemplateRef references the workspace template to instantiate or reuse.
                          The template name is required when enabled is true unless the controller
                          is configured with a provider-specific default template.
                        properties:
                          name:
                            description: Name is the name of the workspace template.
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of the workspace template and claim.
                              It defaults to the Task namespace, or the controller namespace when configured.
                            type: string
                        type: object
                      workspaceSlot:
                        default: default
                        description: WorkspaceSlot names one independently reusable
                          workspace within a Session.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: classRef cannot be combined with legacy enabled, provider,
                        template, pool, cleanup, boot, snapshot, or hibernation settings
                      rule: '!has(self.classRef) || (!has(self.provider) && !has(self.templateRef)
                        && !has(self.poolRef) && (!has(self.enabled) || !self.enabled)
                        && !has(self.cleanupPolicy) && (!has(self.boot) || !self.boot)
                        && !has(self.snapshot) && !has(self.hibernation))'
                type: object
              failedRunsHistoryLimit:
                default: 1
                description: FailedRunsHistoryLimit is the number of failed child
                  tasks to retain.
                format: int32
                minimum: 0
                type: integer
              image:
                description: Image is the container image to run for the task
                type: string
              priorTaskRef:
                description: |-
                  PriorTaskRef references a previously completed task whose diff should be
                  applied to the workspace before this task begins execution.
                properties:
                  name:
                    description: Name is the name of the prior task
                    type: string
                  namespace:
                    description: Namespace is the namespace of the prior task (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              priority:
                default: 500
                description: Priority is the queue priority (0-1000, higher = more
                  urgent)
                format: int32
                maximum: 1000
                minimum: 0
                type: integer
              prompt:
                description: Prompt is the task-specific prompt (used with agentRef)
                type: string
              requestedBy:
                description: |-
                  RequestedBy records the verified identity that created the task.
                  This field is populated by the API server and is immutable.
                properties:
                  email:
                    description: Email is the email claim, if present.
                    type: string
                  groups:
                    description: Groups are verified group values, if present.
                    items:
                      type: string
                    type: array
                  issuer:
                    description: Issuer is the token issuer that authenticated the
                      requester.
                    type: string
                  roles:
                    description: Roles are verified role or scope values, if present.
                    items:
                      type: string
                    type: array
                  subject:
                    description: Subject is the verified subject claim.
                    type: string
                  username:
                    description: Username is the verified username, if present.
                    type: string
                type: object
              resources:
                description: Resources defines the compute resources for the task
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              retryPolicy:
                description: RetryPolicy defines the retry behavior for failed tasks
                properties:
                  backoffMultiplier:
                    default: 2
                    description: BackoffMultiplier is the exponential backoff multiplier
                    type: number
                  initialDelay:
                    description: InitialDelay is the initial delay before the first
                      retry
                    type: string
                  maxRetries:
                    default: 0
                    description: MaxRetries is the maximum number of retry attempts
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              schedule:
                description: |-
                  Schedule is a cron expression for recurring tasks (e.g., "0 */6 * * *").
                  When set, the controller creates child Task CRs on each cron tick.
                type: string
              secretRef:
                description: SecretRef references a Kubernetes Secret containing credentials
                properties:
                  name:
                    description: Name is the name of the Secret
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Secret (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              sessionRef:
                description: SessionRef references a session for conversation continuity
                properties:
                  append:
                    default: true
                    description: Append indicates whether to append task messages
                      to the session transcript
                    type: boolean
                  create:
                    default: false
                    description: Create indicates whether to create the session if
                      it doesn't exist
                    type: boolean
                  maxMessages:
                    default: 50
                    description: MaxMessages is the maximum number of messages to
                      load from session
                    format: int32
                    minimum: 1
                    type: integer
                  name:
                    description: 'Name is the session identifier (ConfigMap: session-<name>)'
                    type: string
                  promptIncluded:
                    description: |-
                      PromptIncluded reports that the current Task prompt is already the final user message in the bounded transcript.
                      Workers must not append prompt a second time when this is true.
                    type: boolean
                  throughMessageId:
                    description: |-
                      ThroughMessageID limits transcript loading to the logical history at and before this stable message ID.
                      Gateway-created Tasks use it so later queued user messages cannot enter an earlier turn.
                    maxLength: 256
                    type: string
                required:
                - name
                type: object
              startingDeadlineSeconds:
                default: 100
                description: |-
                  StartingDeadlineSeconds is the deadline in seconds for starting a missed scheduled run.
                  If the schedule is missed by more than this many seconds, the run is skipped.
                format: int64
                type: integer
              successfulRunsHistoryLimit:
                default: 3
                description: SuccessfulRunsHistoryLimit is the number of successful
                  child tasks to retain.
                format: int32
                minimum: 0
                type: integer
              suspend:
                description: |-
                  Suspend tells the controller to suspend subsequent scheduled runs.
                  It does not apply to already started child tasks. Defaults to false.
                type: boolean
              timeZone:
                description: |-
                  TimeZone is the IANA time zone for the schedule (e.g., "America/New_York").
                  Defaults to UTC if not set.
                type: string
              timeout:
                description: Timeout is the maximum duration for the task
                type: string
              transaction:
                description: |-
                  Transaction records verified transaction-token metadata for audit correlation.
                  This field is populated by the API server and is immutable.
                properties:
                  audience:
                    description: Audience lists the verified token audience values.
                    items:
                      type: string
                    type: array
                  context:
                    additionalProperties:
                      type: string
                    description: Context contains allowlisted, non-sensitive transaction
                      context fields for audit.
                    type: object
                  contextDigest:
                    description: ContextDigest is a SHA256 digest of the full transaction
                      context.
                    type: string
                  id:
                    description: ID is the verified transaction identifier claim.
                    type: string
                  issuer:
                    description: Issuer is the token issuer that authenticated the
                      transaction.
                    type: string
                  profile:
                    description: Profile is the context-token profile that authenticated
                      the request.
                    type: string
                  requesterContextDigest:
                    description: RequesterContextDigest is a SHA256 digest of the
                      full requester context.
                    type: string
                  requestingWorkload:
                    description: RequestingWorkload is the verified workload that
                      requested the transaction.
                    type: string
                  scope:
                    description: Scope is the original verified scope string.
                    type: string
                  scopes:
                    description: Scopes lists parsed scope values from the verified
                      scope string.
                    items:
                      type: string
                    type: array
                  subject:
                    description: Subject is the verified subject claim.
                    type: string
                type: object
              type:
                description: 'Type specifies the task type: "container" or "ai"'
                enum:
                - container
                - ai
                - agent
                type: string
              webhookURL:
                description: WebhookURL is the URL to call when the task completes
                type: string
              workspace:
                description: |-
                  Workspace defines the canonical repository workspace, intent, credentials,
                  and publication request. Agent Tasks that omit intent are interpreted as
                  read by controller logic; an omitted intent preserves existing container behavior.
                properties:
                  allowedPaths:
                    description: |-
                      AllowedPaths restricts publishable workspace changes to these path globs or
                      directory prefixes ending in /**. Empty allows every otherwise-safe path.
                      It is not supported for container Tasks.
                    items:
                      type: string
                    maxItems: 256
                    type: array
                  branch:
                    description: Branch is the source branch to check out.
                    maxLength: 255
                    type: string
                  createPR:
                    default: false
                    description: |-
                      CreatePR explicitly requests pull request reconciliation after branch publication.
                      Branch push remains the minimum durable delivery when false. It is supported only
                      for agent Tasks using the trusted ACP publisher boundary.
                    type: boolean
                  denyRepositoryControlPaths:
                    description: |-
                      DenyRepositoryControlPaths rejects workflow, RBAC, and chart-secret paths
                      before publication even when AllowedPaths is empty or otherwise matches.
                      It is not supported for container Tasks.
                    type: boolean
                  expectedRemoteSHA:
                    description: |-
                      ExpectedRemoteSHA requires the publication branch to exist at this exact
                      commit before publication. Empty means the branch must be absent. It is
                      supported only for agent Tasks using the trusted ACP publisher boundary.
                    pattern: ^([a-f0-9]{40}|[a-f0-9]{64})$
                    type: string
                  forgeCredentialRef:
                    description: |-
                      ForgeCredentialRef references the forge API credential used only for pull
                      request reconciliation when createPR=true.
                    properties:
                      key:
                        default: token
                        description: |-
                          Key is the Secret data key containing one bearer token or one complete
                          Authorization header. It defaults to "token" when omitted.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[A-Za-z0-9._-]+$
                        type: string
                      name:
                        description: Name is the name of the Secret in the Task namespace.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  gitRepo:
                    description: |-
                      GitRepo is the source repository URL cloned by the clean-room workspace boundary.
                      Credentials must not be embedded in the URL.
                    maxLength: 2048
                    type: string
                  intent:
                    description: |-
                      Intent declares whether the verified workspace must remain unchanged or may
                      produce a publication artifact. It is immutable for the lifetime of the Task.
                      Agent Tasks that omit intent are interpreted as read by controller logic;
                      omitted intent preserves the existing behavior of container Tasks.
                    enum:
                    - read
                    - write
                    type: string
                  maxChangedFiles:
                    description: |-
                      MaxChangedFiles bounds the total changed, deleted, and symlink paths accepted
                      from the trusted supervisor before publication. Zero uses the runtime limit.
                      It is not supported for container Tasks.
                    format: int32
                    minimum: 1
                    type: integer
                  prBaseBranch:
                    description: PRBaseBranch is the upstream branch targeted when
                      CreatePR is true.
                    maxLength: 255
                    type: string
                  publicationCredentialRef:
                    description: |-
                      PublicationCredentialRef references the target-repository write credential
                      used only for the exact CAS push. It is never used to clone the source.
                    properties:
                      key:
                        default: token
                        description: |-
                          Key is the Secret data key containing one bearer token or one complete
                          Authorization header. It defaults to "token" when omitted.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[A-Za-z0-9._-]+$
                        type: string
                      name:
                        description: Name is the name of the Secret in the Task namespace.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  publicationGitRepo:
                    description: |-
                      PublicationGitRepo is the repository URL whose branch receives an exact CAS publication.
                      Credentials must not be embedded in the URL.
                    maxLength: 2048
                    type: string
                  publicationReadCredentialRef:
                    description: |-
                      PublicationReadCredentialRef references the target-repository read
                      credential used only for preflight and independent post-push verification.
                    properties:
                      key:
                        default: token
                        description: |-
                          Key is the Secret data key containing one bearer token or one complete
                          Authorization header. It defaults to "token" when omitted.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[A-Za-z0-9._-]+$
                        type: string
                      name:
                        description: Name is the name of the Secret in the Task namespace.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  publicationRepository:
                    description: |-
                      PublicationRepository is the optional URL-derived identity for
                      PublicationGitRepo. When set, it must match the normalized credential-free
                      URL; for GitHub, use provider "github" and ID "github.com/owner/repo".
                    properties:
                      id:
                        description: |-
                          ID is the canonical credential-free URL identity and must match the
                          corresponding repository URL after normalization. For GitHub, use
                          "github.com/owner/repo"; GitHub GraphQL node IDs are not accepted.
                        maxLength: 512
                        minLength: 1
                        type: string
                      provider:
                        description: Provider identifies the source-control provider
                          or forge.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,62}$
                        type: string
                    required:
                    - id
                    - provider
                    type: object
                  pushBranch:
                    description: |-
                      PushBranch is the publication branch. For write Tasks the controller derives
                      a full-entropy Task- or Session-owned branch when this is omitted.
                    maxLength: 255
                    type: string
                  readCredentialRef:
                    description: |-
                      ReadCredentialRef references the one-operation clone/read credential Secret.
                      The Secret is resolved only by the clean-room workspace boundary.
                    properties:
                      key:
                        default: token
                        description: |-
                          Key is the Secret data key containing one bearer token or one complete
                          Authorization header. It defaults to "token" when omitted.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[A-Za-z0-9._-]+$
                        type: string
                      name:
                        description: Name is the name of the Secret in the Task namespace.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  ref:
                    description: Ref is a specific source git ref, commit SHA, or
                      tag to check out.
                    maxLength: 512
                    type: string
                  rejectBinaryFiles:
                    description: |-
                      RejectBinaryFiles rejects changed file content that is not valid text. It is
                      not supported for container Tasks.
                    type: boolean
                  rejectSecretLikeContent:
                    description: |-
                      RejectSecretLikeContent applies Orka's generic secret detector to changed
                      paths and file contents before publication. It is not supported for container Tasks.
                    type: boolean
                  sourceRepository:
                    description: |-
                      SourceRepository is the optional URL-derived identity for GitRepo. When set,
                      it must match the normalized credential-free URL; for GitHub, use provider
                      "github" and ID "github.com/owner/repo".
                    properties:
                      id:
                        description: |-
                          ID is the canonical credential-free URL identity and must match the
                          corresponding repository URL after normalization. For GitHub, use
                          "github.com/owner/repo"; GitHub GraphQL node IDs are not accepted.
                        maxLength: 512
                        minLength: 1
                        type: string
                      provider:
                        description: Provider identifies the source-control provider
                          or forge.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,62}$
                        type: string
                    required:
                    - id
                    - provider
                    type: object
                  subPath:
                    description: SubPath is a subdirectory within the source repository
                      used as workspace root.
                    maxLength: 1024
                    type: string
                type: object
                x-kubernetes-validations:
                - message: createPR requires write workspace intent
                  rule: '!self.createPR || self.intent == ''write'''
                - message: gitRepo must not contain embedded credentials, query parameters,
                    or fragments
                  rule: '!has(self.gitRepo) || (!self.gitRepo.matches(''(?i)^[A-Za-z][A-Za-z0-9+.-]*://[^/]*@'')
                    && !self.gitRepo.contains(''?'') && !self.gitRepo.contains(''#''))'
                - message: publicationGitRepo must not contain embedded credentials,
                    query parameters, or fragments
                  rule: '!has(self.publicationGitRepo) || (!self.publicationGitRepo.matches(''(?i)^[A-Za-z][A-Za-z0-9+.-]*://[^/]*@'')
                    && !self.publicationGitRepo.contains(''?'') && !self.publicationGitRepo.contains(''#''))'
            required:
            - type
            type: object
            x-kubernetes-validations:
            - message: requestedBy is immutable
              rule: has(self.requestedBy) == has(oldSelf.requestedBy) && (!has(self.requestedBy)
                || self.requestedBy == oldSelf.requestedBy)
            - message: transaction is immutable
              rule: has(self.transaction) == has(oldSelf.transaction) && (!has(self.transaction)
                || self.transaction == oldSelf.transaction)
            - message: type is immutable
              rule: self.type == oldSelf.type
            - message: effective workspace intent is immutable
              rule: '(has(self.workspace) && has(self.workspace.intent) ? self.workspace.intent
                : (self.type == ''agent'' ? ''read'' : self.type)) == (has(oldSelf.workspace)
                && has(oldSelf.workspace.intent) ? oldSelf.workspace.intent : (oldSelf.type
                == ''agent'' ? ''read'' : oldSelf.type))'
            - message: agent prompt is immutable
              rule: self.type != 'agent' || (has(self.prompt) == has(oldSelf.prompt)
                && (!has(self.prompt) || self.prompt == oldSelf.prompt))
            - message: agentRef is immutable for agent Tasks
              rule: self.type != 'agent' || (has(self.agentRef) == has(oldSelf.agentRef)
                && (!has(self.agentRef) || self.agentRef == oldSelf.agentRef))
            - message: agentRuntime is immutable for agent Tasks
              rule: self.type != 'agent' || (has(self.agentRuntime) == has(oldSelf.agentRuntime)
                && (!has(self.agentRuntime) || self.agentRuntime == oldSelf.agentRuntime))
            - message: sessionRef is immutable for agent Tasks
              rule: self.type != 'agent' || (has(self.sessionRef) == has(oldSelf.sessionRef)
                && (!has(self.sessionRef) || self.sessionRef == oldSelf.sessionRef))
            - message: workspace is immutable for agent Tasks
              rule: self.type != 'agent' || (has(self.workspace) == has(oldSelf.workspace)
                && (!has(self.workspace) || self.workspace == oldSelf.workspace))
            - message: timeout is immutable for agent Tasks
              rule: self.type != 'agent' || (has(self.timeout) == has(oldSelf.timeout)
                && (!has(self.timeout) || self.timeout == oldSelf.timeout))
            - message: session workspace reuse requires spec.sessionRef
              rule: '!has(self.execution) || !has(self.execution.workspace) || self.execution.workspace.reusePolicy
                != ''session'' || has(self.sessionRef)'
            - message: container Tasks do not support workspace.expectedRemoteSHA
              rule: self.type != 'container' || !has(self.workspace) || !has(self.workspace.expectedRemoteSHA)
            - message: container Tasks do not support workspace.createPR
              rule: self.type != 'container' || !has(self.workspace) || (!has(self.workspace.createPR)
                || !self.workspace.createPR)
            - message: container Tasks do not support clean-room workspace publication
                policies
              rule: self.type != 'container' || !has(self.workspace) || (!has(self.workspace.maxChangedFiles)
                && (!has(self.workspace.allowedPaths) || self.workspace.allowedPaths.size()
                == 0) && (!has(self.workspace.denyRepositoryControlPaths) || !self.workspace.denyRepositoryControlPaths)
                && (!has(self.workspace.rejectBinaryFiles) || !self.workspace.rejectBinaryFiles)
                && (!has(self.workspace.rejectSecretLikeContent) || !self.workspace.rejectSecretLikeContent))
            - message: custom-image container Tasks do not support workspace.pushBranch
                publication
              rule: self.type != 'container' || !has(self.workspace) || !has(self.workspace.pushBranch)
                || self.workspace.pushBranch.size() == 0 || !has(self.image) || self.image.size()
                == 0
          status:
            description: TaskStatus defines the observed state of Task
            properties:
              agentExecutionBinding:
                description: |-
                  AgentExecutionBinding is the authoritative, write-once, immutable
                  execution route for this agent Task.
                properties:
                  agent:
                    description: AgentExecutionAgentRef pins the exact Agent identity
                      resolved at binding.
                    properties:
                      generation:
                        format: int64
                        minimum: 1
                        type: integer
                      name:
                        minLength: 1
                        type: string
                      namespace:
                        minLength: 1
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - generation
                    - name
                    - namespace
                    - uid
                    type: object
                  backend:
                    description: AgentExecutionBackend identifies the isolated execution
                      dispatcher backend.
                    enum:
                    - harness-wrapper
                    - runtime-pool
                    - external-endpoint
                    type: string
                  backendControl:
                    description: |-
                      BackendControl is absent only on legacy-adopted and legacy-cleanup-only
                      bindings created by the sealed migration inventory before durable backend
                      modes existed for the workload.
                    properties:
                      admittedMode:
                        description: AdmittedMode is the effective backend mode observed
                          at admission.
                        enum:
                        - enabled
                        - closing
                        - drain-only
                        - disabled
                        type: string
                      generation:
                        format: int64
                        minimum: 1
                        type: integer
                      modeRevision:
                        format: int64
                        minimum: 1
                        type: integer
                      name:
                        minLength: 1
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - admittedMode
                    - generation
                    - modeRevision
                    - name
                    - uid
                    type: object
                  bindingDigest:
                    description: |-
                      BindingDigest is the canonical digest of this binding; every durable
                      demand, attempt, turn, Session lease, publication, and cleanup record
                      copies it.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  boundAt:
                    format: date-time
                    type: string
                  contractVersion:
                    description: ContractVersion is the frozen execution protocol
                      for the Task lifetime.
                    enum:
                    - orka.harness.v1
                    - orka.harness.v2
                    type: string
                  migrationInventoryID:
                    description: |-
                      MigrationInventoryID binds legacy cleanup-only authority to the exact
                      sealed classification inventory that admitted it. It is absent from all
                      executable and ordinary legacy-adopted bindings.
                    maxLength: 253
                    minLength: 1
                    type: string
                  mode:
                    description: AgentExecutionBindingMode bounds what the binding
                      authorizes.
                    enum:
                    - execute
                    - cleanup-only
                    type: string
                  policy:
                    description: |-
                      Policy freezes the evaluated v1 compatibility policy. Required for
                      newly-bound v1 bindings; absent for v2 bindings.
                    properties:
                      digest:
                        pattern: ^sha256:[a-f0-9]{64}$
                        type: string
                      generation:
                        format: int64
                        minimum: 1
                        type: integer
                      name:
                        minLength: 1
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - digest
                    - generation
                    - name
                    - uid
                    type: object
                  provenance:
                    description: AgentExecutionBindingProvenance records how the binding
                      was established.
                    enum:
                    - newly-bound
                    - legacy-adopted
                    - legacy-cleanup-only
                    type: string
                  runtimeProfileDigest:
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  runtimeProfileDigestSchemaVersion:
                    enum:
                    - 1
                    format: int32
                    type: integer
                  runtimeRef:
                    description: |-
                      AgentExecutionRuntimeRef pins a referenced AgentRuntime identity. UID and
                      generation are recorded only for legacy cleanup-only bindings whose live
                      object may no longer exist.
                    properties:
                      generation:
                        format: int64
                        minimum: 1
                        type: integer
                      name:
                        minLength: 1
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    type: object
                  runtimeType:
                    description: RuntimeType is the built-in runtime type, empty for
                      runtimeRef bindings.
                    enum:
                    - claude
                    - codex
                    - copilot
                    - opencode
                    type: string
                  schemaVersion:
                    enum:
                    - 1
                    format: int32
                    type: integer
                  snapshot:
                    description: AgentExecutionSnapshotRef links the immutable non-secret
                      execution snapshot.
                    properties:
                      digest:
                        pattern: ^sha256:[a-f0-9]{64}$
                        type: string
                      id:
                        description: ID is the snapshot identity in the form <task-uid>/sha256:<digest>.
                        maxLength: 512
                        minLength: 1
                        type: string
                      schemaVersion:
                        enum:
                        - 1
                        format: int32
                        type: integer
                    required:
                    - digest
                    - id
                    - schemaVersion
                    type: object
                  task:
                    description: AgentExecutionBindingTaskRef pins the bound Task
                      identity.
                    properties:
                      boundSpecGeneration:
                        description: BoundSpecGeneration is the Task spec generation
                          frozen into the snapshot.
                        format: int64
                        minimum: 1
                        type: integer
                      namespaceUID:
                        description: |-
                          NamespaceUID is the UID of the Task namespace, preventing same-name
                          namespace recreation from satisfying old identities.
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - boundSpecGeneration
                    - namespaceUID
                    - uid
                    type: object
                required:
                - backend
                - bindingDigest
                - boundAt
                - contractVersion
                - mode
                - provenance
                - schemaVersion
                - snapshot
                - task
                type: object
                x-kubernetes-validations:
                - message: the harness-wrapper backend requires an orka.harness.v1
                    binding
                  rule: self.backend != 'harness-wrapper' || self.contractVersion
                    == 'orka.harness.v1'
                - message: the runtime-pool backend requires an orka.harness.v2 binding
                  rule: self.backend != 'runtime-pool' || self.contractVersion ==
                    'orka.harness.v2'
                - message: legacy-cleanup-only provenance requires cleanup-only mode
                  rule: self.provenance != 'legacy-cleanup-only' || self.mode == 'cleanup-only'
                - message: migrationInventoryID is required only for legacy-cleanup-only
                    provenance
                  rule: 'self.provenance == ''legacy-cleanup-only'' ? has(self.migrationInventoryID)
                    : !has(self.migrationInventoryID)'
              agentExecutionNoExecution:
                description: |-
                  AgentExecutionNoExecution records authoritative proof that this Task has
                  no route-specific executor state; common cleanup only.
                properties:
                  evidenceDigest:
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  migrationInventoryID:
                    description: |-
                      MigrationInventoryID names the sealed inventory that proved the absence
                      of route-specific state.
                    maxLength: 253
                    minLength: 1
                    type: string
                  recordedAt:
                    format: date-time
                    type: string
                  schemaVersion:
                    enum:
                    - 1
                    format: int32
                    type: integer
                  state:
                    description: AgentExecutionNoExecutionState is the proven no-route-state
                      disposition.
                    enum:
                    - UnboundNoExecution
                    type: string
                required:
                - evidenceDigest
                - migrationInventoryID
                - recordedAt
                - schemaVersion
                - state
                type: object
              agentExecutionQuarantine:
                description: |-
                  AgentExecutionQuarantine records immutable mixed or unprovable route
                  evidence; the Task admits no new execution until adjudicated.
                properties:
                  migrationInventoryID:
                    maxLength: 253
                    minLength: 1
                    type: string
                  reason:
                    description: AgentExecutionQuarantineReason is a bounded quarantine
                      reason code.
                    enum:
                    - MixedV1V2Evidence
                    - AmbiguousLegacyEvidence
                    - UnclassifiedAgent
                    - UnprovableSessionLineage
                    - PostCutoffBinding
                    - MissingBindingReservation
                    - StaleModeRevision
                    type: string
                  recordedAt:
                    format: date-time
                    type: string
                  schemaVersion:
                    enum:
                    - 1
                    format: int32
                    type: integer
                  v1EvidenceDigest:
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  v2EvidenceDigest:
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                required:
                - migrationInventoryID
                - reason
                - recordedAt
                - schemaVersion
                type: object
              agentExecutionResolutionRef:
                description: |-
                  AgentExecutionResolutionRef is the immutable reference to the Applied
                  adjudication that resolved this Task's quarantine or blocked state.
                properties:
                  action:
                    description: |-
                      AgentExecutionAdjudicationAction is a one-way operator resolution. Actions
                      never authorize prompt replay, publication replay, or protocol mutation.
                      The coexistence API currently admits only cleanup actions whose exact
                      Applied resolution can be consumed by route-aware finalization. The other
                      constants below are reserved until their required receipt, break-glass, or
                      destination-lineage evidence is represented by the API.
                    enum:
                    - CleanupV1
                    - CleanupV2
                    - CleanupBoth
                    type: string
                  adjudicationName:
                    maxLength: 253
                    minLength: 1
                    type: string
                  adjudicationUID:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                  appliedAt:
                    format: date-time
                    type: string
                  operationDigest:
                    description: OperationDigest matches the applying adjudication's
                      status.operationDigest.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  resolutionDigest:
                    description: |-
                      ResolutionDigest is the canonical digest of this resolution reference and
                      matches the adjudication's status.resolutionRefDigest.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                required:
                - action
                - adjudicationName
                - adjudicationUID
                - appliedAt
                - operationDigest
                - resolutionDigest
                type: object
              attempts:
                description: Attempts is the number of attempts made
                format: int32
                type: integer
              childTasks:
                description: ChildTasks tracks delegated child tasks (for coordinator
                  agents)
                items:
                  description: ChildTaskStatus tracks the status of a delegated child
                    task
                  properties:
                    agent:
                      description: Agent is the agent handling the child task
                      type: string
                    name:
                      description: Name is the name of the child task
                      type: string
                    phase:
                      description: Phase is the current phase of the child task
                      enum:
                      - Pending
                      - Running
                      - Finalizing
                      - Succeeded
                      - Failed
                      - Scheduled
                      - Cancelled
                      type: string
                    result:
                      description: Result is the result from the child task (if completed)
                      type: string
                  required:
                  - agent
                  - name
                  - phase
                  type: object
                type: array
              completionTime:
                description: CompletionTime is when the task completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the Task
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              delivery:
                description: Delivery reports trusted workspace validation and publication
                  reconciliation.
                properties:
                  artifactDigest:
                    description: ArtifactDigest is the durable content-addressed workspace
                      delta digest.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  branch:
                    description: Branch is the publication branch without a refs/heads/
                      prefix.
                    maxLength: 255
                    type: string
                  expectedCommitSHA:
                    description: ExpectedCommitSHA is the exact Orka-owned commit
                      prepared for publication.
                    pattern: ^([a-f0-9]{40}|[a-f0-9]{64})$
                    type: string
                  lastTransitionTime:
                    description: LastTransitionTime is the last durable delivery-state
                      transition time.
                    format: date-time
                    type: string
                  message:
                    description: Message contains bounded, sanitized delivery context.
                    maxLength: 1024
                    type: string
                  outcome:
                    description: Outcome is set only after delivery reaches a terminal
                      classification.
                    enum:
                    - NotRequested
                    - VerifiedExact
                    - DeliveredSuperseded
                    - ReadValidated
                    - NoChange
                    - CancelledBeforePublish
                    - ReadOnlyWorkspaceModified
                    - DeliveryConflict
                    - CredentialBlocked
                    - PublicationOutcomeUnknown
                    type: string
                  prReceipt:
                    description: PRReceipt is present only when createPR was explicitly
                      requested and reconciled.
                    properties:
                      baseBranch:
                        description: BaseBranch is the reconciled pull request base
                          branch.
                        maxLength: 255
                        type: string
                      headBranch:
                        description: HeadBranch is the reconciled pull request head
                          branch.
                        maxLength: 255
                        type: string
                      headSHA:
                        description: HeadSHA is the exact observed pull request head
                          commit.
                        pattern: ^([a-f0-9]{40}|[a-f0-9]{64})$
                        type: string
                      id:
                        description: ID is the provider's durable pull request identifier.
                        maxLength: 512
                        minLength: 1
                        type: string
                      number:
                        description: Number is the provider's numeric pull request
                          number when available.
                        format: int64
                        minimum: 1
                        type: integer
                      state:
                        description: State is the provider-observed pull request state.
                        maxLength: 64
                        type: string
                      url:
                        description: URL is the canonical user-facing pull request
                          URL. It must not contain credentials.
                        maxLength: 2048
                        type: string
                    required:
                    - id
                    type: object
                  publicationID:
                    description: PublicationID is the durable identity reused for
                      reconciliation of the same artifact.
                    maxLength: 253
                    pattern: ^[A-Za-z0-9][A-Za-z0-9._:-]{0,252}$
                    type: string
                  publicationRepository:
                    description: PublicationRepository is the canonical repository
                      whose branch was reconciled.
                    properties:
                      id:
                        description: |-
                          ID is the canonical credential-free URL identity and must match the
                          corresponding repository URL after normalization. For GitHub, use
                          "github.com/owner/repo"; GitHub GraphQL node IDs are not accepted.
                        maxLength: 512
                        minLength: 1
                        type: string
                      provider:
                        description: Provider identifies the source-control provider
                          or forge.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,62}$
                        type: string
                    required:
                    - id
                    - provider
                    type: object
                  reason:
                    description: Reason is a stable machine-readable explanation for
                      State or Outcome.
                    maxLength: 128
                    pattern: ^[A-Za-z][A-Za-z0-9._-]{0,127}$
                    type: string
                  remoteBeforeSHA:
                    description: |-
                      RemoteBeforeSHA is the exact publication ref observed before the CAS push.
                      Nil means not yet observed; a pointer to the empty string records explicit
                      absence; a non-empty value records the observed object ID.
                    pattern: ^(|[a-f0-9]{40}|[a-f0-9]{64})$
                    type: string
                  sourceRepository:
                    description: SourceRepository is the canonical repository from
                      which the workspace baseline was created.
                    properties:
                      id:
                        description: |-
                          ID is the canonical credential-free URL identity and must match the
                          corresponding repository URL after normalization. For GitHub, use
                          "github.com/owner/repo"; GitHub GraphQL node IDs are not accepted.
                        maxLength: 512
                        minLength: 1
                        type: string
                      provider:
                        description: Provider identifies the source-control provider
                          or forge.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,62}$
                        type: string
                    required:
                    - id
                    - provider
                    type: object
                  startingSHA:
                    description: StartingSHA is the verified source baseline before
                      prompt execution.
                    pattern: ^([a-f0-9]{40}|[a-f0-9]{64})$
                    type: string
                  state:
                    description: State is the current durable delivery state.
                    enum:
                    - NotRequested
                    - Validating
                    - Preparing
                    - Prepared
                    - Publishing
                    - Verifying
                    - VerifiedExact
                    - DeliveredSuperseded
                    - ReadValidated
                    - NoChange
                    - CancelledBeforePublish
                    - ReadOnlyWorkspaceModified
                    - DeliveryConflict
                    - CredentialBlocked
                    - PublicationOutcomeUnknown
                    type: string
                  supersedingRemoteSHA:
                    description: SupersedingRemoteSHA is the verified descendant that
                      superseded ExpectedCommitSHA.
                    pattern: ^([a-f0-9]{40}|[a-f0-9]{64})$
                    type: string
                  treeSHA:
                    description: TreeSHA is the deterministic clean-room tree written
                      by the publisher.
                    pattern: ^([a-f0-9]{40}|[a-f0-9]{64})$
                    type: string
                  verifiedRemoteSHA:
                    description: VerifiedRemoteSHA is the independently observed remote
                      branch head.
                    pattern: ^([a-f0-9]{40}|[a-f0-9]{64})$
                    type: string
                type: object
                x-kubernetes-validations:
                - message: delivery outcome requires the matching terminal state
                  rule: '!has(self.outcome) || (has(self.state) && self.state == self.outcome)'
                - message: terminal delivery state requires an outcome
                  rule: '!has(self.state) || !(self.state in [''NotRequested'', ''VerifiedExact'',
                    ''DeliveredSuperseded'', ''ReadValidated'', ''NoChange'', ''CancelledBeforePublish'',
                    ''ReadOnlyWorkspaceModified'', ''DeliveryConflict'', ''CredentialBlocked'',
                    ''PublicationOutcomeUnknown'']) || has(self.outcome)'
              execution:
                description: |-
                  Execution reports the durable execution state and terminal outcome for the
                  current attempt. Phase remains the compatibility projection.
                properties:
                  agentRuntimeName:
                    description: |-
                      AgentRuntimeName is the namespaced external orka.harness.v2 registration
                      selected for this attempt. It is mutually exclusive with RuntimePoolName.
                    maxLength: 253
                    type: string
                  agentRuntimeUID:
                    description: |-
                      AgentRuntimeUID is the immutable external AgentRuntime UID fenced into the
                      attempt selection.
                    maxLength: 253
                    type: string
                  attempt:
                    description: Attempt is the one-based Task execution attempt represented
                      by this status.
                    format: int32
                    minimum: 1
                    type: integer
                  controllerEpoch:
                    description: ControllerEpoch is the durable controller epoch fencing
                      this attempt.
                    format: int64
                    minimum: 0
                    type: integer
                  forgeCredentialResourceVersion:
                    description: |-
                      ForgeCredentialResourceVersion freezes the forge-only Secret version used
                      for pull request reconciliation.
                    maxLength: 253
                    type: string
                  lastTransitionTime:
                    description: LastTransitionTime is the last durable execution-state
                      transition time.
                    format: date-time
                    type: string
                  message:
                    description: Message contains bounded, sanitized execution context.
                    maxLength: 1024
                    type: string
                  outcome:
                    description: Outcome is set only after execution reaches a terminal
                      classification.
                    enum:
                    - Succeeded
                    - Failed
                    - Cancelled
                    - OutcomeUnknown
                    type: string
                  promptID:
                    description: PromptID is the durable prompt identity used for
                      submission and settlement.
                    maxLength: 253
                    type: string
                  publicationCredentialResourceVersion:
                    description: |-
                      PublicationCredentialResourceVersion freezes the target-write Secret
                      version selected at reservation without exposing credential material.
                    maxLength: 253
                    type: string
                  publicationReadCredentialResourceVersion:
                    description: |-
                      PublicationReadCredentialResourceVersion freezes the target-read Secret
                      version used for preflight and independent verification.
                    maxLength: 253
                    type: string
                  readCredentialResourceVersion:
                    description: |-
                      ReadCredentialResourceVersion freezes the read credential Secret version
                      selected at reservation without exposing credential material.
                    maxLength: 253
                    type: string
                  reason:
                    description: Reason is a stable machine-readable explanation for
                      State or Outcome.
                    maxLength: 128
                    pattern: ^[A-Za-z][A-Za-z0-9._-]{0,127}$
                    type: string
                  requestDigest:
                    description: RequestDigest is the canonical immutable prompt request
                      digest.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  runtimeInstanceID:
                    description: RuntimeInstanceID is the exact selected supervisor
                      Pod UID plus boot identity.
                    maxLength: 512
                    type: string
                  runtimePoolName:
                    description: RuntimePoolName is the namespaced logical pool selected
                      for this attempt.
                    maxLength: 253
                    type: string
                  runtimePoolUID:
                    description: RuntimePoolUID is the immutable pool UID fenced into
                      runtime requests.
                    maxLength: 253
                    type: string
                  runtimeSessionCleanupDigest:
                    description: |-
                      RuntimeSessionCleanupDigest is the controller-owned proof that the exact
                      RuntimeSession requiring retirement was deleted or its immutable runtime
                      instance was replaced. Users may read but cannot mutate the Task status subresource.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  runtimeSessionGeneration:
                    description: RuntimeSessionGeneration is the monotonic profile/session
                      generation.
                    format: int64
                    minimum: 0
                    type: integer
                  runtimeSessionMCPDigest:
                    description: |-
                      RuntimeSessionMCPDigest binds the complete non-secret effective MCP policy
                      and descriptor configuration to a pending or reusable Session generation.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  runtimeSessionProfileDigest:
                    description: |-
                      RuntimeSessionProfileDigest freezes the immutable runtime behavior bound to
                      a pending or reusable Session generation.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  runtimeSessionRecreationPending:
                    description: |-
                      RuntimeSessionRecreationPending records that the exact generation is being
                      created or replaced and must be reconciled before a different request may
                      reuse that identity.
                    type: boolean
                  runtimeSessionSupervisorBootID:
                    description: |-
                      RuntimeSessionSupervisorBootID freezes the supervisor boot that owns a
                      pending or reusable Session generation.
                    maxLength: 512
                    type: string
                  runtimeSessionUID:
                    description: RuntimeSessionUID is the stable controller-owned
                      Session execution identity.
                    maxLength: 253
                    type: string
                  runtimeSessionWorkspaceDigest:
                    description: |-
                      RuntimeSessionWorkspaceDigest binds a reusable Session generation to the
                      exact repository, source ref, verified baseline, intent, and relative root.
                      It contains no credential material.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  state:
                    description: State is the current durable execution state.
                    enum:
                    - Queued
                    - Reserved
                    - SessionStarting
                    - Planned
                    - Submitting
                    - SubmittedUnknown
                    - Accepted
                    - Running
                    - Settling
                    - Succeeded
                    - Failed
                    - Cancelled
                    - OutcomeUnknown
                    type: string
                type: object
                x-kubernetes-validations:
                - message: execution outcome requires the matching terminal state
                  rule: '!has(self.outcome) || (has(self.state) && self.state == self.outcome)'
                - message: terminal execution state requires an outcome
                  rule: '!has(self.state) || !(self.state in [''Succeeded'', ''Failed'',
                    ''Cancelled'', ''OutcomeUnknown'']) || has(self.outcome)'
              executionOutcome:
                description: |-
                  ExecutionOutcome records the immutable outcome of a non-ACP workload before
                  provider-neutral execution-workspace finalization completes.
                properties:
                  attempt:
                    description: Attempt is the Task attempt that produced this outcome.
                    format: int32
                    minimum: 1
                    type: integer
                  message:
                    description: Message contains sanitized execution context.
                    type: string
                  phase:
                    allOf:
                    - enum:
                      - Pending
                      - Running
                      - Finalizing
                      - Succeeded
                      - Failed
                      - Scheduled
                      - Cancelled
                    - enum:
                      - Succeeded
                      - Failed
                      - Cancelled
                    description: Phase is the terminal workload execution phase.
                    type: string
                  recordedAt:
                    description: RecordedAt is when Orka durably recorded the execution
                      outcome.
                    format: date-time
                    type: string
                  resultRef:
                    description: ResultRef indicates whether the corresponding result
                      was persisted.
                    properties:
                      available:
                        description: Available indicates whether a result has been
                          stored for this task
                        type: boolean
                    required:
                    - available
                    type: object
                required:
                - attempt
                - phase
                - recordedAt
                type: object
              executionWorkspace:
                description: |-
                  ExecutionWorkspace reports the provider-neutral lifecycle state for a
                  requested execution workspace. Provider-native identifiers and credentials
                  are intentionally omitted.
                properties:
                  attachedEpoch:
                    description: AttachedEpoch is the attachment epoch enforced for
                      this Task.
                    format: int64
                    type: integer
                  classRef:
                    description: ClassRef is the provider-neutral class selected for
                      controller-first execution.
                    properties:
                      name:
                        description: Name is the class name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  cleanupPolicy:
                    description: CleanupPolicy is the resolved cleanup policy.
                    enum:
                    - delete
                    - retain
                    type: string
                  conditions:
                    description: Conditions project generic workspace admission, readiness,
                      attachment, and finalization state.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  density:
                    description: Density reports non-secret actor and worker counts
                      for the workspace provider.
                    properties:
                      actorCount:
                        description: ActorCount is the number of actors reported by
                          the provider.
                        format: int32
                        type: integer
                      actorsPerWorker:
                        description: ActorsPerWorker is ActorCount divided by WorkerCount,
                          formatted as a decimal string.
                        type: string
                      runningActorCount:
                        description: RunningActorCount is the number of actors currently
                          running on workers.
                        format: int32
                        type: integer
                      suspendedActorCount:
                        description: SuspendedActorCount is the number of actors currently
                          suspended.
                        format: int32
                        type: integer
                      workerCount:
                        description: WorkerCount is the number of workers reported
                          by the provider.
                        format: int32
                        type: integer
                    type: object
                  lastUpdateTime:
                    description: LastUpdateTime is the last time workspace status
                      was updated.
                    format: date-time
                    type: string
                  message:
                    description: Message contains sanitized lifecycle context.
                    type: string
                  phase:
                    description: Phase is the provider-neutral lifecycle phase.
                    enum:
                    - Pending
                    - Ready
                    - Released
                    - Retained
                    - Deleted
                    - Failed
                    type: string
                  placement:
                    description: Placement reports non-secret runtime placement metadata
                      for the workspace.
                    properties:
                      workerNamespace:
                        description: WorkerNamespace is the namespace containing the
                          selected worker pod.
                        type: string
                      workerPodName:
                        description: WorkerPodName is the selected worker pod name
                          when available.
                        type: string
                      workerPool:
                        description: WorkerPool is the provider's worker-pool name
                          when available.
                        type: string
                    type: object
                  provider:
                    description: Provider is the resolved legacy workspace backend.
                    enum:
                    - agent-sandbox
                    - substrate
                    type: string
                  reason:
                    description: Reason is the provider-neutral lifecycle reason.
                    enum:
                    - WorkspacePending
                    - WorkspaceClaimed
                    - WorkspaceReady
                    - WorkspaceReleased
                    - WorkspaceRetained
                    - WorkspaceDeleted
                    - WorkspaceValidationFailed
                    - WorkspaceAttachmentLocked
                    - WorkspaceClaimFailed
                    - WorkspaceReadinessFailed
                    - WorkspaceHandoffFailed
                    - WorkspaceCommandFailed
                    - WorkspaceSecretScrubFailed
                    - WorkspaceCleanupFailed
                    - WorkspaceStatusUpdateFailed
                    type: string
                  resumeLatency:
                    description: ResumeLatency is the observed time spent resuming
                      the workspace until it was ready.
                    type: string
                  reusePolicy:
                    description: ReusePolicy is the resolved reuse policy.
                    enum:
                    - none
                    - session
                    type: string
                  reused:
                    description: Reused reports whether an existing workspace was
                      reattached.
                    type: boolean
                  state:
                    description: State is the provider-neutral concrete workspace
                      state.
                    type: string
                  templateRef:
                    description: TemplateRef is the resolved workspace template.
                    properties:
                      name:
                        description: Name is the name of the workspace template.
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the workspace template and claim.
                          It defaults to the Task namespace, or the controller namespace when configured.
                        type: string
                    type: object
                  workspaceRef:
                    description: WorkspaceRef identifies the concrete ExecutionWorkspace
                      in the Task namespace.
                    properties:
                      name:
                        description: Name is the ExecutionWorkspace name.
                        type: string
                      uid:
                        description: UID is the immutable ExecutionWorkspace UID.
                        type: string
                    required:
                    - name
                    type: object
                type: object
              harnessRuntime:
                description: |-
                  HarnessRuntime records the controller-resolved harness v1 runtime target
                  for an in-flight agent turn. It intentionally stores only non-secret
                  routing metadata and Secret references, never bearer values. Compatibility
                  surface for harness v1 bindings.
                properties:
                  attempt:
                    description: Attempt is the durable harness v1 attempt number.
                    format: int32
                    minimum: 1
                    type: integer
                  authRefField:
                    description: AuthRefField is the Secret data field selected when
                      the turn started.
                    type: string
                  authRefName:
                    description: AuthRefName is the Secret name selected when the
                      turn started.
                    type: string
                  authRefResourceVersion:
                    description: |-
                      AuthRefResourceVersion is the auth Secret resourceVersion validated
                      before starting the turn.
                    type: string
                  cancelRequestedAt:
                    description: |-
                      CancelRequestedAt records a durable cancellation request. Cancellation
                      remains nonterminal until a terminal frame or ledger receipt is observed.
                    format: date-time
                    type: string
                  contractVersion:
                    description: ContractVersion is the Orka harness contract version
                      used for the turn.
                    type: string
                  controllerEpoch:
                    description: ControllerEpoch records the fenced controller epoch
                      driving the attempt.
                    format: int64
                    minimum: 0
                    type: integer
                  endpoint:
                    description: Endpoint is the non-secret harness base URL selected
                      when the turn started.
                    type: string
                  lastEventSeq:
                    description: LastEventSeq is the highest durably mapped harness
                      frame sequence.
                    format: int64
                    minimum: 0
                    type: integer
                  lastTransitionTime:
                    description: |-
                      LastTransitionTime is the last durable v1 attempt transition projected to
                      the Task.
                    format: date-time
                    type: string
                  message:
                    description: Message is bounded, sanitized execution context.
                    maxLength: 1024
                    type: string
                  outcome:
                    description: Outcome is set only for a terminal harness v1 attempt.
                    enum:
                    - Succeeded
                    - Failed
                    - Cancelled
                    - OutcomeUnknown
                    type: string
                  reason:
                    description: Reason is a bounded machine-readable terminal reason
                      code.
                    maxLength: 256
                    type: string
                  requestDigest:
                    description: |-
                      RequestDigest binds the canonical StartTurn request admitted by the
                      durable wrapper ledger.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  runtimeGeneration:
                    description: RuntimeGeneration is the AgentRuntime generation
                      selected when the turn started.
                    format: int64
                    type: integer
                  runtimeName:
                    description: |-
                      RuntimeName is the runtime name advertised by the harness capabilities
                      and sent in turn metadata.
                    type: string
                  runtimeRefName:
                    description: |-
                      RuntimeRefName is the AgentRuntime name for custom runtimeRef turns.
                      Empty means built-in CLI wrapper.
                    type: string
                  runtimeSessionID:
                    description: RuntimeSessionID is the deterministic, non-secret
                      v1 runtime-session identity.
                    type: string
                  state:
                    description: State is the durable harness v1 attempt state projected
                      for operators.
                    enum:
                    - Queued
                    - Reserved
                    - SessionStarting
                    - Planned
                    - Submitting
                    - SubmittedUnknown
                    - Accepted
                    - Running
                    - Settling
                    - Succeeded
                    - Failed
                    - Cancelled
                    - OutcomeUnknown
                    type: string
                  terminalReceiptDigest:
                    description: TerminalReceiptDigest identifies the authoritative
                      terminal or unknown receipt.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  turnID:
                    description: TurnID is the deterministic, non-secret harness turn
                      identity.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: terminal harness state requires an outcome
                  rule: '!has(self.state) || !(self.state in [''Succeeded'', ''Failed'',
                    ''Cancelled'', ''OutcomeUnknown'']) || has(self.outcome)'
                - message: harness outcome requires a terminal state
                  rule: '!has(self.outcome) || self.state in [''Succeeded'', ''Failed'',
                    ''Cancelled'', ''OutcomeUnknown'']'
                - message: OutcomeUnknown harness state requires OutcomeUnknown outcome
                  rule: self.state != 'OutcomeUnknown' || self.outcome == 'OutcomeUnknown'
              iteration:
                description: |-
                  Iteration is the current autonomous loop iteration (0-based).
                  Only used when the task's coordination config has autonomous mode enabled.
                format: int32
                type: integer
              jobName:
                description: JobName is the name of the Kubernetes Job running the
                  task
                type: string
              lastScheduleTime:
                description: LastScheduleTime is the last time a child task was created
                  for a scheduled run.
                format: date-time
                type: string
              message:
                description: Message provides additional status information
                type: string
              nextScheduleTime:
                description: NextScheduleTime is the next time a child task will be
                  created.
                format: date-time
                type: string
              phase:
                description: Phase is the current phase of the task
                enum:
                - Pending
                - Running
                - Finalizing
                - Succeeded
                - Failed
                - Scheduled
                - Cancelled
                type: string
              resultRef:
                description: ResultRef indicates whether a result is available
                properties:
                  available:
                    description: Available indicates whether a result has been stored
                      for this task
                    type: boolean
                required:
                - available
                type: object
              startTime:
                description: StartTime is when the task started running
                format: date-time
                type: string
              webhookDelivered:
                description: WebhookDelivered indicates whether the webhook was successfully
                  called
                type: boolean
            type: object
            x-kubernetes-validations:
            - message: executionOutcome is immutable once recorded
              rule: '!has(oldSelf.executionOutcome) || self.executionOutcome == oldSelf.executionOutcome'
            - message: agentExecutionBinding is write-once and immutable
              rule: '!has(oldSelf.agentExecutionBinding) || (has(self.agentExecutionBinding)
                && self.agentExecutionBinding == oldSelf.agentExecutionBinding)'
            - message: agentExecutionNoExecution is write-once and immutable
              rule: '!has(oldSelf.agentExecutionNoExecution) || (has(self.agentExecutionNoExecution)
                && self.agentExecutionNoExecution == oldSelf.agentExecutionNoExecution)'
            - message: agentExecutionQuarantine evidence is never cleared or rewritten
              rule: '!has(oldSelf.agentExecutionQuarantine) || (has(self.agentExecutionQuarantine)
                && self.agentExecutionQuarantine == oldSelf.agentExecutionQuarantine)'
            - message: agentExecutionResolutionRef is append-once and immutable
              rule: '!has(oldSelf.agentExecutionResolutionRef) || (has(self.agentExecutionResolutionRef)
                && self.agentExecutionResolutionRef == oldSelf.agentExecutionResolutionRef)'
            - message: a Task cannot be both bound and proven no-execution
              rule: '!(has(self.agentExecutionBinding) && has(self.agentExecutionNoExecution))'
            - message: a quarantined Task cannot hold an execution binding
              rule: '!(has(self.agentExecutionBinding) && has(self.agentExecutionQuarantine))'
            - message: no-execution and quarantine dispositions are mutually exclusive
              rule: '!(has(self.agentExecutionNoExecution) && has(self.agentExecutionQuarantine))'
            - message: a v1-bound Task cannot acquire new v2 execution or delivery
                state
              rule: '!has(self.agentExecutionBinding) || self.agentExecutionBinding.contractVersion
                != ''orka.harness.v1'' || ((!has(self.execution) || (has(oldSelf.execution)
                && self.execution == oldSelf.execution)) && (!has(self.delivery) ||
                (has(oldSelf.delivery) && self.delivery == oldSelf.delivery)))'
            - message: a v2-bound Task cannot acquire new v1 harness state
              rule: '!has(self.agentExecutionBinding) || self.agentExecutionBinding.contractVersion
                != ''orka.harness.v2'' || !has(self.harnessRuntime) || (has(oldSelf.harnessRuntime)
                && self.harnessRuntime == oldSelf.harnessRuntime)'
        type: object
        x-kubernetes-validations:
        - message: Task spec is immutable after execution authority or a migration
            disposition is recorded
          rule: '!has(oldSelf.status) || (!(has(oldSelf.status.agentExecutionBinding)
            || has(oldSelf.status.agentExecutionNoExecution) || has(oldSelf.status.agentExecutionQuarantine))
            || self.spec == oldSelf.spec)'
    served: true
    storage: true
    subresources:
      status: {}
