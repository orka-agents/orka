---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: fakeproviderconfigs.fake.workspace.orka.ai
spec:
  group: fake.workspace.orka.ai
  names:
    categories:
    - orka
    kind: FakeProviderConfig
    listKind: FakeProviderConfigList
    plural: fakeproviderconfigs
    singular: fakeproviderconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          FakeProviderConfig is the cluster-scoped configuration identity used by the
          development-only fake workspace provider.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              FakeProviderConfigSpec is intentionally empty. The in-memory development
              provider needs only a concrete cluster-scoped identity for reference and
              REST-mapping conformance.
            type: object
        type: object
    served: true
    storage: true
