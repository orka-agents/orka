# Replace with a real key locally; do not commit real provider keys.
apiVersion: v1
kind: Secret
metadata:
  name: fibey-llm-secret
  namespace: orka-system
type: Opaque
stringData:
  api-key: "REPLACE_ME"
