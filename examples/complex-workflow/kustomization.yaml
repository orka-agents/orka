apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secrets.yaml
  - tools.yaml
  - skills.yaml
  - agents.yaml
  # Tasks are applied separately as needed
  # - tasks.yaml

# Common labels for all resources
commonLabels:
  app.kubernetes.io/part-of: orka-complex-workflow
  app.kubernetes.io/example: "true"
