apiVersion: core.orka.ai/v1alpha1
kind: AgentRuntime
metadata:
  name: fibey-http-runtime
spec:
  contractVersion: orka.harness.v1
  deployment:
    mode: external-endpoint
    endpoint: http://fibey-http-runtime.default.svc.cluster.local:8080
    transportSecurity: insecure-cluster-local-http
  clientAuth:
    bearerTokenSecretRef:
      name: fibey-http-runtime-token
      key: token
  capabilities:
    toolExecutionModes:
    - observed
    supportsCancel: true
    supportsRuntimeSessions: true
