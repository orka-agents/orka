apiVersion: core.orka.ai/v1alpha1
kind: Agent
metadata:
  name: fibey-custom
spec:
  runtime:
    runtimeRef:
      name: fibey-agentkit
  systemPrompt:
    inline: |
      You are Fibey's custom incident scout. Produce a concise dossier and propose any side-effecting work order instead of executing it directly.
