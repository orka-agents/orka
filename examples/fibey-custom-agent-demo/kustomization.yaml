apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - agent-agentkit.yaml
  - agent-foundry.yaml
# Register the configured runtimes separately, then create a fresh Task with
# switch-backend.sh. Applying this directory does not start an inference.
