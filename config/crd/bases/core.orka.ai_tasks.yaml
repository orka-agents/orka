---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: tasks.core.orka.ai
spec:
  group: core.orka.ai
  names:
    kind: Task
    listKind: TaskList
    plural: tasks
    singular: task
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Task is the Schema for the tasks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TaskSpec defines the desired state of Task
            properties:
              agentRef:
                description: AgentRef references an Agent CRD for configuration
                properties:
                  name:
                    description: Name is the name of the Agent
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Agent (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              agentRuntime:
                description: AgentRuntime contains task-level overrides for agent
                  runtime configuration (when type is "agent")
                properties:
                  allowBash:
                    description: AllowBash enables the agent to run bash commands
                      (overrides Agent default)
                    type: boolean
                  allowedTools:
                    description: AllowedTools lists the tools the agent is allowed
                      to use (overrides Agent defaults)
                    items:
                      type: string
                    type: array
                  disallowedTools:
                    description: DisallowedTools lists tools the agent is not allowed
                      to use
                    items:
                      type: string
                    type: array
                  maxTurns:
                    description: MaxTurns limits the number of agent loop iterations
                    format: int32
                    maximum: 1000
                    minimum: 1
                    type: integer
                  workspace:
                    description: Workspace defines the working directory configuration
                    properties:
                      branch:
                        description: Branch is the git branch to checkout
                        type: string
                      forkRepo:
                        description: ForkRepo is the writable fork repository URL
                          for pushing changes
                        type: string
                      gitRepo:
                        description: GitRepo is the repository URL to clone
                        type: string
                      gitSecretRef:
                        description: GitSecretRef references a Secret containing git
                          credentials
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      prBaseBranch:
                        description: PRBaseBranch is the upstream branch to target
                          for pull requests
                        type: string
                      pushBranch:
                        description: |-
                          PushBranch is the remote branch name to push changes to after the agent completes.
                          When set, FinalizeResult will commit and push changes to this branch.
                        type: string
                      ref:
                        description: Ref is a specific git ref (commit SHA, tag) to
                          checkout
                        type: string
                      subPath:
                        description: SubPath is a subdirectory within the repo to
                          use as workspace root
                        type: string
                    type: object
                type: object
              ai:
                description: AI contains AI-specific configuration (when type is "ai")
                properties:
                  maxTokens:
                    description: MaxTokens limits the response length
                    format: int32
                    type: integer
                  model:
                    description: Model is the model identifier - required if providerRef
                      not set
                    type: string
                  prompt:
                    description: Prompt is the user prompt for the AI task
                    type: string
                  provider:
                    description: Provider is the LLM provider (anthropic, openai)
                      - required if providerRef not set
                    enum:
                    - anthropic
                    - openai
                    type: string
                  providerRef:
                    description: |-
                      ProviderRef references a Provider CRD for LLM configuration
                      If set, provider and model fields are optional (defaults from Provider)
                    properties:
                      name:
                        description: Name is the name of the Provider
                        type: string
                      namespace:
                        description: Namespace is the namespace of the Provider (defaults
                          to Task namespace)
                        type: string
                    required:
                    - name
                    type: object
                  skills:
                    description: Skills references Skill CRDs to inject into the agent's
                      system prompt
                    items:
                      description: SkillReference references a Skill CRD by name or
                        inline skill content from a ConfigMap key.
                      properties:
                        configMapRef:
                          description: ConfigMapRef references a ConfigMap key containing
                            skill text
                          properties:
                            key:
                              description: Key is the key within the ConfigMap
                              type: string
                            name:
                              description: Name is the name of the ConfigMap
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        name:
                          description: Name references a Skill CR by name
                          type: string
                      type: object
                    type: array
                  systemPrompt:
                    description: SystemPrompt is an optional system prompt
                    type: string
                  temperature:
                    description: Temperature controls randomness in generation
                    maximum: 2
                    minimum: 0
                    type: number
                  tools:
                    description: Tools lists the tools available for this task
                    items:
                      type: string
                    type: array
                type: object
              args:
                description: Args are the arguments to pass to the command
                items:
                  type: string
                type: array
              command:
                description: Command is the command to run in the container
                items:
                  type: string
                type: array
              concurrencyPolicy:
                default: Forbid
                description: ConcurrencyPolicy specifies how to treat concurrent runs
                  (Allow or Forbid).
                enum:
                - Allow
                - Forbid
                type: string
              env:
                description: Env is a list of environment variables to set in the
                  container
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              failedRunsHistoryLimit:
                default: 1
                description: FailedRunsHistoryLimit is the number of failed child
                  tasks to retain.
                format: int32
                minimum: 0
                type: integer
              image:
                description: Image is the container image to run for the task
                type: string
              priorTaskRef:
                description: |-
                  PriorTaskRef references a previously completed task whose diff should be
                  applied to the workspace before this task begins execution.
                properties:
                  name:
                    description: Name is the name of the prior task
                    type: string
                  namespace:
                    description: Namespace is the namespace of the prior task (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              priority:
                default: 500
                description: Priority is the queue priority (0-1000, higher = more
                  urgent)
                format: int32
                maximum: 1000
                minimum: 0
                type: integer
              prompt:
                description: Prompt is the task-specific prompt (used with agentRef)
                type: string
              resources:
                description: Resources defines the compute resources for the task
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              retryPolicy:
                description: RetryPolicy defines the retry behavior for failed tasks
                properties:
                  backoffMultiplier:
                    default: 2
                    description: BackoffMultiplier is the exponential backoff multiplier
                    type: number
                  initialDelay:
                    description: InitialDelay is the initial delay before the first
                      retry
                    type: string
                  maxRetries:
                    default: 0
                    description: MaxRetries is the maximum number of retry attempts
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              schedule:
                description: |-
                  Schedule is a cron expression for recurring tasks (e.g., "0 */6 * * *").
                  When set, the controller creates child Task CRs on each cron tick.
                type: string
              secretRef:
                description: SecretRef references a Kubernetes Secret containing credentials
                properties:
                  name:
                    description: Name is the name of the Secret
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Secret (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              sessionRef:
                description: SessionRef references a session for conversation continuity
                properties:
                  append:
                    default: true
                    description: Append indicates whether to append task messages
                      to the session transcript
                    type: boolean
                  create:
                    default: false
                    description: Create indicates whether to create the session if
                      it doesn't exist
                    type: boolean
                  maxMessages:
                    default: 50
                    description: MaxMessages is the maximum number of messages to
                      load from session
                    format: int32
                    minimum: 1
                    type: integer
                  name:
                    description: 'Name is the session identifier (ConfigMap: session-<name>)'
                    type: string
                required:
                - name
                type: object
              startingDeadlineSeconds:
                default: 100
                description: |-
                  StartingDeadlineSeconds is the deadline in seconds for starting a missed scheduled run.
                  If the schedule is missed by more than this many seconds, the run is skipped.
                format: int64
                type: integer
              successfulRunsHistoryLimit:
                default: 3
                description: SuccessfulRunsHistoryLimit is the number of successful
                  child tasks to retain.
                format: int32
                minimum: 0
                type: integer
              suspend:
                description: |-
                  Suspend tells the controller to suspend subsequent scheduled runs.
                  It does not apply to already started child tasks. Defaults to false.
                type: boolean
              timeZone:
                description: |-
                  TimeZone is the IANA time zone for the schedule (e.g., "America/New_York").
                  Defaults to UTC if not set.
                type: string
              timeout:
                description: Timeout is the maximum duration for the task
                type: string
              type:
                description: 'Type specifies the task type: "container" or "ai"'
                enum:
                - container
                - ai
                - agent
                type: string
              webhookURL:
                description: WebhookURL is the URL to call when the task completes
                type: string
            required:
            - type
            type: object
          status:
            description: TaskStatus defines the observed state of Task
            properties:
              attempts:
                description: Attempts is the number of attempts made
                format: int32
                type: integer
              childTasks:
                description: ChildTasks tracks delegated child tasks (for coordinator
                  agents)
                items:
                  description: ChildTaskStatus tracks the status of a delegated child
                    task
                  properties:
                    agent:
                      description: Agent is the agent handling the child task
                      type: string
                    name:
                      description: Name is the name of the child task
                      type: string
                    phase:
                      description: Phase is the current phase of the child task
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Scheduled
                      - Cancelled
                      type: string
                    result:
                      description: Result is the result from the child task (if completed)
                      type: string
                  required:
                  - agent
                  - name
                  - phase
                  type: object
                type: array
              completionTime:
                description: CompletionTime is when the task completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the Task
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              iteration:
                description: |-
                  Iteration is the current autonomous loop iteration (0-based).
                  Only used when the task's coordination config has autonomous mode enabled.
                format: int32
                type: integer
              jobName:
                description: JobName is the name of the Kubernetes Job running the
                  task
                type: string
              lastScheduleTime:
                description: LastScheduleTime is the last time a child task was created
                  for a scheduled run.
                format: date-time
                type: string
              message:
                description: Message provides additional status information
                type: string
              nextScheduleTime:
                description: NextScheduleTime is the next time a child task will be
                  created.
                format: date-time
                type: string
              phase:
                description: Phase is the current phase of the task
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Scheduled
                - Cancelled
                type: string
              resultRef:
                description: ResultRef indicates whether a result is available
                properties:
                  available:
                    description: Available indicates whether a result has been stored
                      for this task
                    type: boolean
                required:
                - available
                type: object
              startTime:
                description: StartTime is when the task started running
                format: date-time
                type: string
              webhookDelivered:
                description: WebhookDelivered indicates whether the webhook was successfully
                  called
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
