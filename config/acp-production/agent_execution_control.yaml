apiVersion: core.orka.ai/v1alpha1
kind: AgentExecutionControl
metadata:
  name: cluster
  namespace: orka-system
spec:
  backends:
    v1:
      desiredMode: disabled
    v2:
      desiredMode: enabled
